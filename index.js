
// Provided variables
const num1 = 2;
const num2 = 31;
const num3 = 24;
const num4 = 5;

// 1. Multiply num1 and num2 to get 62
const multiply = num1 * num2;

// 2. Generate a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1; // 1–100, change 100 if you want smaller range

// 3. Calculate remainder of num3 / num4 to get 4
const mod = num3 % num4;

// 4. Find the highest number in a set (must include 20)
const max = Math.max(5, 10, 15, 20);
